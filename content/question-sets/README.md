# Question sets

Production question sets live here as one JSON file per set:

`content/question-sets/<course-code>/<set-id>.json`

The first release intentionally contains no question-set JSON files. Follow the repository-level `AGENTS.md` when adding content.
