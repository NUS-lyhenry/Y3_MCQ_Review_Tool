# Question assets

Store question images at:

`public/question-assets/<course-code>/<set-id>/<image>.png`

Supported formats are PNG, JPEG, and WebP. Every image reference in a question must use a root-relative `/question-assets/...` path and provide meaningful alternative text.
