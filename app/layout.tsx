import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import 'katex/dist/katex.min.css';
import './globals.css';

import { ReviewProvider } from '@/components/review-provider';
import { QUESTION_SETS } from '@/lib/question-library';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'Y3 MCQ Review',
  description:
    'A focused multiple-choice revision workspace for study and exam practice.',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ReviewProvider questionSets={QUESTION_SETS}>{children}</ReviewProvider>
      </body>
    </html>
  );
}
